@model ManageUsersViewModel

@{
    Layout = "_AdminLayout";
}

<div class="section section-admin">
    <h6>Manage Users</h6>
    @for (int i = 0; i < this.Model.Users.Count(); i++)
    {
        <partial name="UserSummary" model="@this.Model.Users[i]" />
    }
</div>